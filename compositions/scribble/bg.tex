% Shared preamble for the scribble realizer's `bg` / `fg` feature.
% The realizer additionally generates one `\newenvironment{TonartBg<id>}`
% per unique color used in the document — see `bg-color-env-def` in
% scribble.rkt — so all this file does is pull in tcolorbox once.
\usepackage[most]{tcolorbox}
